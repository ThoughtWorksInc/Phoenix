# This file contains generic ssh credentials objects

us-east-ssh:
  private_key: us-east-test.pem   # This should refer to a relative location where the private key is stored.
                                  # If using AWS, this should match the keypair name
  login: ubuntu                   # The login name associated with the key


